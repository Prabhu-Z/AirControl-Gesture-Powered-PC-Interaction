# Copy this to .env and adjust if your layout differs.
PORT=5000

# Absolute path to the SAME gestures_config.json that hand_mouse.py reads.
# Default (if unset) is two folders up from server.js, e.g.:
#   C:\hackzen\gesture-config-web\server\server.js
#   -> C:\hackzen\gestures_config.json
# Uncomment and edit if you want to point somewhere else:
# CONFIG_FILE_PATH=C:\hackzen\gestures_config.json
